import React from "react";

export default class Overview extends React.Component<any, any> {
    render() {
        return(
            <a href="customers/add" className="btn btn-primary">add customer</a>
        );
    }
}